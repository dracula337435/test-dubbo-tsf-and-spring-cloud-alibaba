#
dubbo:
  scan:
    base-packages: io.dracula.test.dubbo.tsf.and.alibaba.provider
  protocol:
    name: dubbo
    port: -1
  registry:
    address: spring-cloud://localhost

# 若name在application中，spring-cloud和dubbo均失败
spring:
  application:
    name: test-dubbo-tsf-alibaba-provider